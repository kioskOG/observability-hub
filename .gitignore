# Local AWS POC state (bucket names, region) — do not commit
.observability-poc-aws.state

# Basic auth material — managed in AWS Secrets Manager via External Secrets Operator
**/.htpasswd
loki/.htpasswd
mimir/.htpasswd

# Terragrunt
.terragrunt-cache/
**/.terragrunt-cache/

# Terraform
**/.terraform/
**/.terraform.lock.hcl
**/.terraform.d/

# Terraform state (never commit)
**/*.tfstate
**/*.tfstate.*

# macOS
.DS_Store
# Terragrunt
.terragrunt-cache/
**/.terragrunt-cache/

# Terraform
**/.terraform/
**/.terraform.lock.hcl

# Terraform state (never commit)
**/*.tfstate
**/*.tfstate.*
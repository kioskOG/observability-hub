# Syncs Nginx gateway htpasswd for Loki (key must be ".htpasswd").
# AWS SM: observability-hub/loki-basic-auth → JSON {"htpasswd": "..."}
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: loki-basic-auth
  namespace: loki
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: aws-secrets-manager
    kind: ClusterSecretStore
  target:
    name: loki-basic-auth
    creationPolicy: Owner
    template:
      engineVersion: v2
      data:
        .htpasswd: '{{ trim .htpasswd }}'
  data:
    - secretKey: htpasswd
      remoteRef:
        key: observability-hub/loki-basic-auth
        property: htpasswd
